# Day 1: Python Basics

## What I Learned Today

Today marks the start of my Python journey! Covered the absolute 
fundamentals:

- **Variables** — how to store data using names (e.g., `name = "Dude"`)
- **Values** — the actual data stored in variables (numbers, text, etc.)
- **`print()`** — displaying output to the screen
- **`input()`** — taking input from the user

These basics form the foundation for everything else in Python — 
understanding how to store, retrieve, and display data is the first 
step before diving into more complex logic.

## Mini Project

### Band Name Generator
A simple, fun program that asks the user for their city and their 
pet's name, then combines them to generate a random "band name" idea.

```python
city = input("What's the name of the city you grew up in?\n")
pet_name = input("What's your pet's name?\n")
print(f"Your band name could be {city} {pet_name}")
```

## Key Takeaway
Even though the concepts today were simple, this was a great 
confidence-building start — seeing a working program come together 
from just a few lines of code made the whole journey feel exciting 
and achievable.

---

Excited to keep going — Day 2 onwards, let's build on this! 🐍